# Go build output
/lidarr-metadata-provider
/probe
*.exe
*.test
*.out

# Dataset artifacts and anything unpacked from a MusicBrainz dump.
# These are large, reproducible from the pipeline, and never committed.
/data/
/dumps/
/dist/
*.tar.bz2
*.tar.zst
*.db
*.db-shm
*.db-wal
*.sqlite
*.db.building

# Build-time enrichment cache: image and biography data harvested from
# Wikidata and Wikipedia. Large, reproducible, and refreshed incrementally, so
# it is kept on the build machine rather than committed.
/enrich/

# Local scratch
/scratch/
.env
